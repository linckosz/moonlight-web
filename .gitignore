# Qt/C++ build artifacts
build/
build-*/
*.o
*.obj
*.exe
*.dll
# Exception: prebuilt OpenSSL runtime DLLs are a committed dependency (shipped
# next to mw-server.exe; required by CI artifacts which checkout clean).
!backend/libs/windows/lib/**/*.dll
*.so
*.dylib
moc_*.cpp
qrc_*.cpp
*.moc
ui_*.h
Makefile
Makefile.Debug
Makefile.Release
*.pro.user
*.user
.qtcreator/
*.autosave
.qmake.stash

release/
debug/
backend/release/
backend/debug/

# IDE
.vs/
.vscode/
.idea/
*.swp
*.swo

# Temp
*.log
/tmp/

# Certificates (generated at runtime)
backend/certs/

# Build cache
backend/.cache/

# Agent session results
.claude/results/

# Docs: keep only wiki and screenshots tracked
docs/*
!docs/wiki
!docs/screenshots

# Local benchmark prompt documents (never committed)
docs/application/

# Local dev config (committed once, then ignored)
.moonlightweb-dev

# Private key embedding (never committed)
.env

# Node tooling (frontend lint/format/typecheck)
node_modules/

# Test coverage reports (generated)
frontend/coverage/
backend/tests/cov.xml
backend/tests/covhtml/

# clang-tidy compilation database (generated, machine-absolute paths)
backend/compile_commands.json

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Claude Code / AI agent files (kept on disk, never pushed)
.claude/
CLAUDE.md

# Graphify (share graph outputs, keep local cache/state out)
graphify-out/*
!graphify-out/graph.html
!graphify-out/graph.json
!graphify-out/GRAPH_REPORT.md

# ── Local debug / scratch helpers (kept on disk, never pushed) ──
# Root-level build & debug scripts + scratch output (none belong in the repo)
/*.bat
/*.cmd
/*.ps1
/*.sh
/*.txt
/_explore.js
/list-files-output.md

# Backend debug/build scratch — keep only the dependency build scripts
backend/*.bat
backend/*.cmd
backend/*.ps1
backend/*.sh
!backend/build_libdatachannel.bat
!backend/build_miniupnpc.bat
!backend/build_msvc.bat
!backend/build.sh
!backend/patch_makefile.ps1
backend/scripts/*.ps1
backend/build_output.txt
backend/tmp_build_test.txt
backend/src/server/file_list.txt
backend/src/tmp_list.txt
backend/src/streaming/list_files.sh
backend/installer/MoonlightWeb/
