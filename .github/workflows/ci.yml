# Moonlight-Web — unified CI pipeline.
#
# Single gated pipeline, each stage blocks the next:
#   1. quality + frontend tests — frontend job (Prettier+ESLint then Vitest, one
#      npm install) plus C++ quality (clang-format + cppcheck).
#   2. backend tests — Qt Test + OpenCppCoverage (70% gate), after C++ quality.
#   3. build — Windows x64, Windows ARM64, Linux x64, macOS arm64 (CMake), only
#      once the frontend job and backend tests are green.
#
# Builds never run on a broken commit, so the (expensive) multi-platform matrix
# is only spent on validated code.
name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

# Least-privilege token: this pipeline only reads the repo and writes artifacts.
permissions:
  contents: read

# Cancel superseded PR runs to save CI minutes, but never cancel a push on main
# (those feed releases and must run to completion).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # ──────────────────── 1. QUALITY + FRONTEND TESTS ────────────────────
  # Frontend quality and tests share one job: they're sequential anyway (tests
  # gate on quality) and a single npm install/Node setup serves both.
  frontend:
    name: Frontend (Prettier + ESLint + Vitest, 70% gate)
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Prettier + ESLint
        id: quality
        run: npm run check
      - name: Report quality
        if: failure()
        run: |
          {
            echo "### ❌ Frontend quality failed"
            echo ""
            echo "Run locally and commit the fixes:"
            echo '```bash'
            echo "cd frontend"
            echo "npm run format:fix   # auto-format with Prettier"
            echo "npm run lint:fix     # auto-fix ESLint issues"
            echo '```'
            echo "Remaining ESLint errors must be fixed by hand — see the log above."
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Run tests with coverage
        run: npm run coverage
      - name: Report tests
        # Only when quality passed but tests/coverage failed, so the report points
        # at the right failure.
        if: failure() && steps.quality.outcome == 'success'
        run: |
          {
            echo "### ❌ Frontend tests / coverage gate failed"
            echo ""
            echo "Reproduce locally: \`cd frontend && npm run coverage\`."
            echo "Either a test regressed or coverage dropped below the 70% gate."
          } >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-coverage
          path: frontend/coverage
          if-no-files-found: ignore

  quality-cpp-format:
    name: Quality · C++ clang-format
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      # Pin to the same major.minor as the bundled LLVM (VS2022) to avoid
      # formatting disagreements across clang-format versions.
      - run: pip install "clang-format==19.1.7"
      - name: Check formatting (house style in backend/.clang-format)
        run: |
          git ls-files -- 'backend/src/' | grep -E '\.(cpp|h)$' \
            | xargs clang-format --dry-run --Werror
      - name: Report
        if: failure()
        run: |
          {
            echo "### ❌ C++ formatting failed"
            echo ""
            echo "Apply the house style (backend/.clang-format) and commit:"
            echo '```bash'
            echo "git ls-files -- 'backend/src/' | grep -E '\\.(cpp|h)\$' | xargs clang-format -i"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  quality-cppcheck:
    name: Quality · C++ cppcheck
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y cppcheck
      - name: Run cppcheck
        working-directory: backend
        run: |
          cppcheck --enable=warning --library=qt --std=c++17 --language=c++ \
            --inline-suppr \
            --suppress=missingInclude --suppress=missingIncludeSystem \
            --suppress=unknownMacro \
            --error-exitcode=1 -I src src
      - name: Report
        if: failure()
        run: |
          {
            echo "### ❌ cppcheck found static-analysis issues"
            echo ""
            echo "See the annotated warnings above (null derefs, leaks, uninit vars…)."
            echo "Fix them, or add an inline \`// cppcheck-suppress <id>\` with justification."
          } >> "$GITHUB_STEP_SUMMARY"

  # ────────────────────────── 2. BACKEND TESTS ──────────────────────────
  test-backend:
    name: Tests · Backend (Qt Test + OpenCppCoverage, 70% gate)
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    needs: [quality-cpp-format, quality-cppcheck]
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive # moonlight-common-c provides Limelight.h
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.11.0
          arch: win64_msvc2022_64
          # aqt 3.3.0 doesn't know the new Qt 6.11 per-arch repo layout (404s on
          # Updates.xml); pin the commit that added it. py7zr 1.1.0 avoids the 7z
          # path-sanitization bug (aqtinstall#995) that breaks SBOM extraction.
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b
          py7zrversion: ==1.1.0
          extra: --base https://download.qt.io # official server (fallback mirrors serve incomplete XML)
      - name: Set up MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - name: Install OpenCppCoverage
        run: choco install opencppcoverage -y
      - name: Add OpenCppCoverage to PATH
        shell: pwsh
        run: echo "C:\Program Files\OpenCppCoverage" | Out-File -FilePath $env:GITHUB_PATH -Append
      - name: Run backend TNR + coverage
        shell: cmd
        run: backend\tests\run_coverage.bat
      - name: Report
        if: failure()
        shell: bash
        run: |
          {
            echo "### ❌ Backend tests / coverage gate failed"
            echo ""
            echo "Reproduce on Windows: \`backend\\tests\\run_coverage.bat\`."
            echo "Either a Qt Test regressed or line coverage dropped below the 70% gate."
          } >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-coverage
          path: backend/tests/build/covhtml
          if-no-files-found: ignore

  # ────────────────────────────── 3. BUILD ──────────────────────────────
  build-windows-x64:
    name: Build · Windows x64 (CMake/MSVC)
    # TEMP(debug): runs standalone while the other jobs are disabled below.
    # Restore `needs: [frontend, test-backend]` once the Windows bundle is fixed.
    runs-on: windows-latest
    # Grants access to the MW_* secrets (DNS/ACME config embedded at build time).
    environment: MoonlightWeb
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install Qt 6.11
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.11.0
          arch: win64_msvc2022_64
          modules: qtwebsockets
          # aqt 3.3.0 doesn't know the new Qt 6.11 per-arch repo layout (404s on
          # Updates.xml); pin the commit that added it. py7zr 1.1.0 avoids the 7z
          # path-sanitization bug (aqtinstall#995) that breaks SBOM extraction.
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b
          py7zrversion: ==1.1.0
          extra: --base https://download.qt.io # official server (fallback mirrors serve incomplete XML)
      # Ninja + MSVC env: the runner's Visual Studio major version changes over
      # time, so avoid the version-pinned "Visual Studio NN" generator.
      - name: Set up MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - name: Configure
        # Internet-Access config embedded at build time from repo secrets (no
        # .env shipped). Empty when a secret is unset → LAN-only fallback.
        env:
          MW_DOMAIN: ${{ secrets.MW_DOMAIN }}
          MW_PDNS_URL: ${{ secrets.MW_PDNS_URL }}
          MW_PDNS_TOKEN: ${{ secrets.MW_PDNS_TOKEN }}
          MW_ZEROSSL_EAB_KID: ${{ secrets.MW_ZEROSSL_EAB_KID }}
          MW_ZEROSSL_EAB_HMAC: ${{ secrets.MW_ZEROSSL_EAB_HMAC }}
        run: cmake -S backend -B backend/build/win -G Ninja -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build backend/build/win -j
      # Stage a clean, relocatable bundle: exe + frontend + OpenSSL DLLs
      # (libssl-3-x64 / libcrypto-3-x64), as laid out by the install() rules.
      - name: Install bundle
        run: cmake --install backend/build/win --prefix backend/build/win/dist
      # windeployqt adds the Qt runtime DLLs (Qt6Core/Network/WebSockets/Widgets)
      # + plugins next to the exe inside the bundle.
      - name: Deploy Qt runtime
        run: windeployqt --release backend/build/win/dist/MoonlightWeb.exe
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moonlight-web-windows-x64
          path: backend/build/win/dist
          if-no-files-found: warn

  build-windows-arm64:
    name: Build · Windows ARM64 (CMake/MSVC)
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    needs: [frontend, test-backend]
    runs-on: windows-11-arm
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install OpenSSL (vcpkg, arm64)
        shell: pwsh
        run: vcpkg install openssl:arm64-windows
      - name: Install Qt 6.11 (arm64)
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.11.0
          arch: win64_msvc2022_arm64
          modules: qtwebsockets
          # aqt 3.3.0 doesn't know the new Qt 6.11 per-arch repo layout (404s on
          # Updates.xml); pin the commit that added it. py7zr 1.1.0 avoids the 7z
          # path-sanitization bug (aqtinstall#995) that breaks SBOM extraction.
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b
          py7zrversion: ==1.1.0
          extra: --base https://download.qt.io # official server (fallback mirrors serve incomplete XML)
      - name: Set up MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: arm64
      - name: Configure
        shell: pwsh
        run: >
          cmake -S backend -B backend/build/win-arm64 -G Ninja -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake"
          -DVCPKG_TARGET_TRIPLET=arm64-windows
      - name: Build
        run: cmake --build backend/build/win-arm64 -j
      - name: Deploy Qt runtime
        run: windeployqt --release backend/build/win-arm64/MoonlightWeb.exe
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moonlight-web-windows-arm64
          path: backend/build/win-arm64/MoonlightWeb.exe
          if-no-files-found: warn

  build-linux-x64:
    name: Build · Linux x64 (CMake)
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    needs: [frontend, test-backend]
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build pkg-config \
            libssl-dev
      - name: Install Qt 6.11
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.11.0
          modules: qtwebsockets
          # aqt 3.3.0 doesn't know the new Qt 6.11 per-arch repo layout (404s on
          # Updates.xml); pin the commit that added it. py7zr 1.1.0 avoids the 7z
          # path-sanitization bug (aqtinstall#995) that breaks SBOM extraction.
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b
          py7zrversion: ==1.1.0
          extra: --base https://download.qt.io # official server (fallback mirrors serve incomplete XML)
      - name: Configure
        run: cmake -S backend -B backend/build/linux -G Ninja -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build backend/build/linux -j
      - name: Security TNR (ctest)
        run: ctest --test-dir backend/build/linux --output-on-failure
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moonlight-web-linux-x64
          path: backend/build/linux/MoonlightWeb
          if-no-files-found: warn

  build-macos-arm64:
    name: Build · macOS arm64 (CMake)
    if: ${{ false }} # TEMP(debug): disabled until Windows-x64 bundle is fixed
    needs: [frontend, test-backend]
    runs-on: macos-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install build dependencies
        run: brew install cmake ninja openssl@3 pkg-config
      - name: Install Qt 6.11
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.11.0
          host: mac
          arch: clang_64 # Qt 6.11 mac desktop is a universal (arm64+x64) binary
          modules: qtwebsockets
          # aqt 3.3.0 doesn't know the new Qt 6.11 per-arch repo layout (404s on
          # Updates.xml); pin the commit that added it. py7zr 1.1.0 avoids the 7z
          # path-sanitization bug (aqtinstall#995) that breaks SBOM extraction.
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b
          py7zrversion: ==1.1.0
          extra: --base https://download.qt.io # official server (fallback mirrors serve incomplete XML)
      - name: Configure
        run: cmake -S backend -B backend/build/mac -G Ninja -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build backend/build/mac -j
      - name: Security TNR (ctest)
        run: ctest --test-dir backend/build/mac --output-on-failure
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: moonlight-web-macos-arm64
          path: backend/build/mac/MoonlightWeb
          if-no-files-found: warn
