<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Info.plist for the MoonlightWeb Installer.app plugin bundle. Mirrors Apple's
  "Installer Plug-in" Xcode template: NSPrincipalClass is the framework's own
  InstallerSection, which loads NSMainNibFile; the nib's File's Owner wires its
  firstPane outlet to our MWSunshinePane (and the pane's contentView to a view).
  InstallerSectionTitle is only the sidebar label; the section itself is
  referenced by bundle file name in plugins/InstallerSections.plist.
-->
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key><string>en</string>
    <key>CFBundleExecutable</key><string>MoonlightWebInstaller</string>
    <key>CFBundleIdentifier</key><string>com.moonlightweb.installer.plugin</string>
    <key>CFBundleInfoDictionaryVersion</key><string>6.0</string>
    <key>CFBundleName</key><string>MoonlightWebInstaller</string>
    <key>CFBundlePackageType</key><string>BNDL</string>
    <key>CFBundleShortVersionString</key><string>1.0</string>
    <key>CFBundleVersion</key><string>1</string>
    <key>NSPrincipalClass</key><string>InstallerSection</string>
    <key>NSMainNibFile</key><string>MWSunshinePane</string>
    <key>InstallerSectionTitle</key><string>Sunshine</string>
</dict>
</plist>
