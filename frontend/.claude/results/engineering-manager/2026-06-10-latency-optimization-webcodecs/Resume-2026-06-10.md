# Engineering Manager — Resume 2026-06-10

## Demande
Appliquer 5 modifications d'optimisation de latence pour le rendu `<video>` en mode WebRTC MediaTrack.

## Modifications appliquees

### Fichier : `frontend/js/api/WebRtcMedia.js`
1. **(Ligne ~566)** Remplace le stripping `ulpfec` partiel par un nettoyage complet des lignes ULPFEC, RED et fmtp.
2. **(Ligne ~571)** Ajoute le retrait du NACK de retransmission (garde `nack pli`).
3. **(Ligne ~345)** Ajoute `receiver.setJitterBufferMinimumDelay(0)` dans le handler `ontrack`.
4. **(Ligne ~356)** Ajoute `playoutDelayHint = 0` sur le video element apres `srcObject = stream`.

### Fichier : `frontend/js/ui/StreamView.js`
5. **(Ligne ~610)** Ajoute `playoutDelayHint = 0` sur `this.videoEl` dans la methode `render()` pour le mode webrtc-media.

## Sous-agents
- `frontend-dev` : lecture + ecriture des fichiers (fait)
- `code-reviewer` : revue en cours

## Verdict
En attente de la revue.
