# When set to 1, auto-grants DuckDNS consent at startup, bypassing the GDPR
# consent popup. This is useful for developers testing remote access via a
# domain name (DuckDNS + Let's Encrypt). Not needed for LAN-only development.
MOONLIGHT_DDNS_CONSENT=0
